
#include <stdio.h>

int
main ()
{
  int year;
  printf ("Enter Your year:");
  scanf ("%d", &year);
  if (year%400==0)
    {
      printf ("Yes,this is a leap year");
    }
  else
    {
      printf ("No,this is not a leap year");
    }

  return 0;
}
